const initialState = {

};

export default function guide(state = initialState, action) {
  switch (action.type) {
    case 'CHANGE_DIMENSION':
      return state;
    default:
      return state;
  }
}
